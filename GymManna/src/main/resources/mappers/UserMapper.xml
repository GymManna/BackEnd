<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="myUser">

   <resultMap id="user"
      type="com.mycompany.mygym.user.vo.User">
      <result property="userId" column="user_id" jdbcType="VARCHAR"
         javaType="String" />
      <result property="userPw" column="user_password" jdbcType="VARCHAR"
         javaType="String" />
         <result property="userName" column="user_name" jdbcType="VARCHAR"
         javaType="String" />
   </resultMap>

   <select id="login" resultMap="user">
      select user_id, user_password, user_name
      from user
      WHERE user_id = #{userId} AND user_password = #{userPw}
   </select>

</mapper>